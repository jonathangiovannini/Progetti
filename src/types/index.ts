export interface TeamMember {
    name: string;
    role: string;
    description: string;
    image: string;
    roleColor: string;
    email: string;
}
export interface Mission {
    icon: string;
    title: string;
    description: string;
    color: string;
}